name: MCP Servers
version: 0.0.1
schema: v1
mcpServers:
  - name: PA State Park Search
    command: uv
    args:
      - "--directory"
      - "c:/projects/mcpPAStateParks"
      - "run"
      - "mcpPAStateParks.py"
